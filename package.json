{
  "name": "mq9-reaper",
  "version": "0.1.0",
  "description": "Map target selection app — pin, shortlist, annotate, and review geographic points",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
